# Полное руководство по развертыванию
ок 

## Шаг 1: Подготовка GitHub репозитория

### 1.1 Инициализация Git (если еще не сделано)
```bash
cd /Users/greenlion/PycharmProjects/she_v_1.0
git init
git add .
git commit -m "Initial commit: Shanghai Schedule App"
```

### 1.2 Создание репозитория на GitHub
1. Откройте https://github.com
2. Нажмите "+" → "New repository"
3. Название: `shanghai-schedule` (или любое другое)
4. Оставьте репозиторий приватным или публичным
5. НЕ добавляйте README, .gitignore или лицензию (у нас уже есть)
6. Нажмите "Create repository"

### 1.3 Подключение к GitHub
```bash
git remote add origin https://github.com/ВАШ-USERNAME/shanghai-schedule.git
git branch -M main
git push -u origin main
```

## Шаг 2: Деплой на Render.com

### 2.1 Создание аккаунта
1. Откройте https://render.com
2. Зарегистрируйтесь или войдите через GitHub

### 2.2 Создание Web Service
1. На dashboard нажмите "New +" → "Web Service"
2. Подключите ваш GitHub репозиторий
   - Если репозиторий не виден, нажмите "Configure account"
   - Разрешите доступ Render к вашему репозиторию
3. Выберите ваш репозиторий `shanghai-schedule`

### 2.3 Настройка сервиса
Заполните параметры:

**Basic Settings:**
- Name: `shanghai-schedule` (или любое уникальное имя)
- Region: `Singapore` (ближе к Шанхаю) или `Frankfurt`
- Branch: `main`
- Root Directory: оставьте пустым

**Build Settings:**
- Runtime: `Node`
- Build Command: `npm install`
- Start Command: `npm start`

**Instance Type:**
- Выберите `Free` для начала (можно обновить позже)

**Environment Variables:**
Добавьте следующие переменные:
- Key: `TELEGRAM_BOT_TOKEN`, Value: `7248913086:AAHnu8f8bQfrOCWkwlkmnmjqs2JdTv2BHGU`
- Key: `TELEGRAM_CHAT_ID`, Value: `-1003244054167`
- Key: `NODE_ENV`, Value: `production`

4. Нажмите "Create Web Service"

### 2.4 Ожидание деплоя
- Render автоматически начнет сборку
- Процесс займет 3-5 минут
- Вы увидите логи в реальном времени
- После успешного деплоя получите URL вида: `https://shanghai-schedule-XXXX.onrender.com`

## Шаг 3: Настройка домена на GoDaddy

### 3.1 Получение URL от Render
После успешного деплоя на Render:
1. Скопируйте ваш URL (например, `shanghai-schedule-abcd.onrender.com`)
2. Этот URL понадобится для настройки DNS

### 3.2 Настройка DNS на GoDaddy

#### Вариант A: Использование CNAME (Рекомендуется)

1. Войдите в https://godaddy.com
2. Перейдите в "My Products" → "Domains"
3. Найдите `escortwork.org` и нажмите "DNS"
4. Удалите или отключите существующие A записи для @ или домена
5. Добавьте новую запись:
   ```
   Type: CNAME
   Name: @ (для корневого домена)
   Value: shanghai-schedule-abcd.onrender.com (ваш URL от Render БЕЗ https://)
   TTL: 600 seconds (или оставьте по умолчанию)
   ```
6. Нажмите "Save"

**Примечание:** Некоторые провайдеры не разрешают CNAME для корневого домена. В этом случае используйте Вариант B.

#### Вариант B: Использование A записи

1. В Render перейдите в Settings вашего сервиса
2. Scroll до "Custom Domain"
3. Добавьте `escortwork.org`
4. Render покажет IP адрес для A записи

5. Вернитесь в GoDaddy DNS settings:
   ```
   Type: A
   Name: @ (для корневого домена)
   Value: [IP адрес от Render]
   TTL: 600 seconds
   ```
6. Нажмите "Save"

#### Если нужен поддомен (например, schedule.escortwork.org):
```
Type: CNAME
Name: schedule
Value: shanghai-schedule-abcd.onrender.com
TTL: 600 seconds
```

### 3.3 Настройка Custom Domain в Render

1. В Render dashboard откройте ваш сервис
2. Перейдите в "Settings" tab
3. Scroll до "Custom Domain"
4. Нажмите "Add Custom Domain"
5. Введите: `escortwork.org`
6. Render автоматически настроит HTTPS сертификат (это займет несколько минут)

### 3.4 Проверка
- DNS изменения могут занять от 10 минут до 48 часов
- Обычно работает через 15-30 минут
- Проверить можно командой: `nslookup escortwork.org`
- Откройте https://escortwork.org - должно работать

## Шаг 4: Обновление кода через PyCharm и Git Push

### 4.1 Настройка Git в PyCharm (если еще не настроено)

1. Откройте PyCharm
2. File → Settings (или PyCharm → Preferences на Mac)
3. Version Control → Git
4. Укажите путь к git (обычно находится автоматически)
5. Нажмите "Test" для проверки
6. OK

### 4.2 Работа с Git в PyCharm

#### Коммит изменений:
1. Внесите изменения в файлы
2. В PyCharm откройте панель "Commit" (Alt+0 или Cmd+0)
   - Или: VCS → Commit
3. Выберите файлы для коммита (галочки)
4. Напишите commit message
5. Нажмите "Commit"

#### Push на GitHub:
1. После коммита нажмите "Push" в том же окне
   - Или: VCS → Git → Push (Ctrl+Shift+K или Cmd+Shift+K)
2. Подтвердите push
3. PyCharm отправит изменения на GitHub

### 4.3 Альтернатива: Использование терминала

В PyCharm откройте Terminal (View → Tool Windows → Terminal) и выполните:

```bash
# Проверить статус
git status

# Добавить все измененные файлы
git add .

# Создать коммит
git commit -m "Описание изменений"

# Отправить на GitHub
git push origin main
```

### 4.4 Автоматический деплой на Render

После `git push`:
1. Render автоматически обнаружит изменения
2. Начнется автоматическая пересборка
3. Через 2-3 минуты изменения будут на сайте
4. Можно смотреть прогресс в Render Dashboard → Logs

## Шаг 5: Проверка работы приложения

### 5.1 Тестирование основных функций

1. **Откройте сайт:** https://escortwork.org
2. **Проверьте вход:**
   - Логин: kris, misha, katya, wang, или lion
   - Пароль для всех: 12344321
3. **Проверьте функции:**
   - ✅ Время Шанхая отображается вверху
   - ✅ 3 дня (сегодня, завтра, послезавтра)
   - ✅ Добавление таблиц (111, 222, 333, 555, 666, 888)
   - ✅ Заполнение временных слотов
   - ✅ Адреса на китайском
   - ✅ Журнал изменений (кнопка 📋)
   - ✅ Автообновление каждые 2 секунды

### 5.2 Тестирование Telegram бота

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте команду `/all` - должен прийти скриншот

### 5.3 Проверка на мобильном

1. Откройте сайт на телефоне
2. Проверьте touch-реакцию
3. Убедитесь что всё читаемо и кликабельно

## Шаг 6: Мониторинг и обслуживание

### 6.1 Просмотр логов в Render

1. Откройте Render Dashboard
2. Выберите ваш сервис
3. Вкладка "Logs"
4. Здесь видны все ошибки и события

### 6.2 Автоматическая архивация

- Каждый день в 4:00 утра (время Шанхая):
  - Создается скриншот текущего дня
  - Отправляется в Telegram группу
  - Данные самого старого дня удаляются
  - Появляются новые 3 дня

### 6.3 Если что-то не работает

**Проблема: Сайт не открывается**
- Проверьте DNS настройки в GoDaddy
- Подождите до 48 часов для propagation
- Проверьте логи в Render

**Проблема: Бот не отвечает**
- Проверьте логи в Render
- Убедитесь что бот запущен (в логах должно быть "Telegram bot initialized")

**Проблема: Данные не сохраняются**
- Render Free tier перезагружается после неактивности
- Рассмотрите переход на Starter план ($7/месяц) для постоянной работы

## Полезные команды

### Просмотр статуса Git
```bash
git status
git log --oneline
```

### Отмена последнего коммита (если еще не сделан push)
```bash
git reset --soft HEAD~1
```

### Принудительный push (осторожно!)
```bash
git push -f origin main
```

### Откат к предыдущей версии
```bash
git log  # найдите commit hash
git checkout [commit-hash]
git checkout main  # вернуться обратно
```

## Безопасность

⚠️ **Важно:** Токен бота и Chat ID встроены в код. Для production рекомендуется:

1. Переместить их в переменные окружения Render
2. Использовать Environment Variables в Render Dashboard

## Поддержка

Если возникли вопросы:
1. Проверьте логи в Render
2. Проверьте GitHub Actions (если настроены)
3. Проверьте DNS на https://dnschecker.org
