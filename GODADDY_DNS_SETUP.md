# 🌐 Настройка домена escortwork.org на GoDaddy

## Быстрая инструкция

### Шаг 1: Войдите в GoDaddy

1. Откройте https://godaddy.com
2. Нажмите "Sign In" (Войти)
3. Введите свои учетные данные

### Шаг 2: Откройте управление DNS

1. Нажмите на ваше имя (правый верхний угол) → "My Products"
2. Найдите домен **escortwork.org**
3. Нажмите кнопку **"DNS"** или **"Manage DNS"**

### Шаг 3: Получите URL Render

После деплоя на Render вы получите URL:
```
https://shanghai-schedule.onrender.com
```

Или другой URL, который вы выбрали при создании сервиса.

### Шаг 4: Добавьте CNAME запись

#### Вариант A: CNAME для @ (корневой домен)

Если GoDaddy позволяет CNAME для @:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| CNAME | @ | shanghai-schedule.onrender.com | 600 |

**Как добавить:**
1. Нажмите **"Add"** или **"Add Record"**
2. Выберите тип: **CNAME**
3. Name (Имя): введите **@**
4. Value (Значение): введите **shanghai-schedule.onrender.com**
5. TTL: оставьте по умолчанию или введите **600**
6. Нажмите **"Save"**

#### Вариант B: CNAME для www

Если @ не работает, добавьте для www:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| CNAME | www | shanghai-schedule.onrender.com | 600 |

**Затем добавьте перенаправление:**
1. В GoDaddy найдите раздел "Forwarding" или "Redirects"
2. Добавьте redirect: `escortwork.org` → `www.escortwork.org`

#### Вариант C: A-запись (если CNAME не работает)

1. Получите IP адрес Render:
   ```bash
   nslookup shanghai-schedule.onrender.com
   ```

   Пример результата:
   ```
   Name: shanghai-schedule.onrender.com
   Address: 216.24.57.253
   ```

2. Добавьте A-запись:

| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 216.24.57.253 | 600 |

⚠️ **Внимание:** IP адреса Render могут меняться. CNAME предпочтительнее!

### Шаг 5: Удалите конфликтующие записи

Перед добавлением новой записи, удалите старые:

**Удалите следующие записи для @:**
- Старые A-записи для @
- Старые AAAA-записи для @
- Старые CNAME-записи для @

**Как удалить:**
1. Найдите запись в списке
2. Нажмите значок карандаша ✏️ или корзины 🗑️
3. Подтвердите удаление

### Шаг 6: Добавьте домен в Render

1. Зайдите в Render Dashboard: https://dashboard.render.com
2. Выберите ваш сервис **shanghai-schedule**
3. Перейдите в **Settings** → **Custom Domain**
4. Нажмите **"Add Custom Domain"**
5. Введите: **escortwork.org** (без www)
6. Нажмите **"Save"**

Render покажет инструкцию по настройке DNS. Следуйте ей, если что-то не работает.

### Шаг 7: Проверьте настройку

DNS обновление занимает от 15 минут до 48 часов (обычно 1-2 часа).

**Проверить DNS:**
```bash
nslookup escortwork.org
```

Должен показать Render IP или CNAME.

**Проверить сайт:**
```bash
curl https://escortwork.org/api/health
```

Должен вернуть:
```json
{"status":"ok","time":"...","bot":true,"screenshots":"html2canvas"}
```

**Открыть в браузере:**
```
https://escortwork.org
```

### Шаг 8: SSL сертификат

Render автоматически настроит бесплатный SSL от Let's Encrypt.

**Проверить SSL:**
1. Откройте https://escortwork.org
2. Кликните на замок 🔒 в адресной строке
3. Должно показать: "Connection is secure"

Если SSL не работает сразу:
- Подождите 5-10 минут
- Render автоматически выпустит сертификат

---

## 🔍 Устранение неполадок

### Проблема 1: "DNS_PROBE_FINISHED_NXDOMAIN"

**Причина:** DNS еще не обновился

**Решение:**
- Подождите 1-2 часа
- Проверьте правильность записи в GoDaddy
- Очистите DNS кеш:
  ```bash
  # Windows
  ipconfig /flushdns

  # Mac
  sudo dscacheutil -flushcache

  # Linux
  sudo systemd-resolve --flush-caches
  ```

### Проблема 2: "NET::ERR_CERT_COMMON_NAME_INVALID"

**Причина:** SSL сертификат еще не выпущен

**Решение:**
- Подождите 5-10 минут
- Проверьте в Render Settings → Custom Domain
- Статус должен быть "Verified"

### Проблема 3: GoDaddy не позволяет CNAME для @

**Решение:**
Используйте Вариант C (A-запись) или Вариант B (www + redirect)

### Проблема 4: Сайт показывает 404

**Решение:**
- Проверьте, что в Render добавлен домен escortwork.org
- Проверьте DNS: `nslookup escortwork.org`
- Проверьте логи в Render Dashboard

### Проблема 5: HTTP работает, HTTPS не работает

**Решение:**
- Подождите выпуска SSL (5-10 минут)
- Проверьте Custom Domain в Render (должен быть зеленый чекмарк ✓)

---

## 📋 Итоговая конфигурация DNS

После настройки ваша DNS конфигурация должна выглядеть так:

### Вариант 1 (CNAME):
| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| CNAME | @ | shanghai-schedule.onrender.com | 600 |
| CNAME | www | shanghai-schedule.onrender.com | 600 |

### Вариант 2 (A-запись):
| Тип | Имя | Значение | TTL |
|-----|-----|----------|-----|
| A | @ | 216.24.57.XXX | 600 |
| CNAME | www | shanghai-schedule.onrender.com | 600 |

---

## ✅ Проверочный чеклист

- [ ] Вошли в GoDaddy
- [ ] Открыли DNS управление для escortwork.org
- [ ] Удалили старые A/CNAME записи для @
- [ ] Добавили новую CNAME запись
- [ ] Добавили домен в Render Custom Domain
- [ ] Подождали 15-30 минут
- [ ] nslookup escortwork.org показывает правильный адрес
- [ ] https://escortwork.org открывается
- [ ] SSL сертификат работает (замок 🔒)
- [ ] /api/health возвращает OK

---

## 🎯 Готово!

После выполнения всех шагов:

✅ **escortwork.org** → работает
✅ **www.escortwork.org** → работает (опционально)
✅ **HTTPS** → работает (SSL от Let's Encrypt)
✅ **Telegram бот** → подключен
✅ **Скриншоты** → работают

**Время распространения DNS:** 15 минут - 2 часа (обычно 1 час)

---

## 📞 Поддержка

Если ничего не помогает:
1. Проверьте логи в Render Dashboard
2. Свяжитесь с поддержкой GoDaddy
3. Проверьте Community форум Render

**Полезные ссылки:**
- GoDaddy DNS Help: https://www.godaddy.com/help/dns-management-19199
- Render Custom Domains: https://render.com/docs/custom-domains
