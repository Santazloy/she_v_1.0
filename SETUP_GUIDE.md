# 🚀 Полное руководство по настройке Shanghai Schedule

## 📋 Содержание
1. [Что нужно](#что-нужно)
2. [Установка на локальной машине](#установка-на-локальной-машине)
3. [Настройка на Render.com](#настройка-на-rendercom)
4. [Настройка домена на GoDaddy](#настройка-домена-на-godaddy)
5. [Работа с GitHub](#работа-с-github)
6. [Обновление через PyCharm](#обновление-через-pycharm)

---

## 🎯 Что нужно

### Аккаунты и сервисы
- ✅ GitHub аккаунт (уже есть: `Santazloy/she_v_1.0`)
- ✅ Render.com аккаунт (бесплатный)
- ✅ GoDaddy аккаунт с доменом `escortwork.org`
- ✅ Telegram бот (уже создан)
- ✅ PyCharm или другая IDE

### Технические требования
- Node.js 18+
- Git
- Интернет

---

## 💻 Установка на локальной машине

### 1. Клонируйте репозиторий из GitHub

```bash
cd ~/PycharmProjects
git clone https://github.com/Santazloy/she_v_1.0.git
cd she_v_1.0
```

### 2. Установите зависимости

```bash
npm install
```

### 3. Создайте файл `.env`

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot Configuration
TELEGRAM_BOT_TOKEN=7248913086:AAHnu8f8bQfrOCWkwlkmnmjqs2JdTv2BHGU
TELEGRAM_CHAT_ID=-1003244054167

# Server Configuration
NODE_ENV=development
PORT=3000
```

### 4. Запустите сервер локально

```bash
npm start
```

Откройте браузер: `http://localhost:3000`

**Логины для входа:**
- kris / 12344321
- misha / 12344321
- katya / 12344321
- wang / 12344321
- lion / 12344321

---

## ☁️ Настройка на Render.com

### Шаг 1: Подключите GitHub репозиторий

1. Зайдите на https://render.com
2. Нажмите **"New +"** → **"Web Service"**
3. Подключите ваш GitHub аккаунт
4. Выберите репозиторий: `Santazloy/she_v_1.0`
5. Нажмите **"Connect"**

### Шаг 2: Настройте сервис

**Basic Settings:**
- Name: `shanghai-schedule` (или любое другое имя)
- Region: `Singapore` или `Frankfurt` (ближайший к Китаю)
- Branch: `main`
- Root Directory: (оставьте пустым)

**Build Settings:**
- Build Command: `./render-build.sh`
- Start Command: `npm start`

**Instance Type:**
- Free (512MB RAM) - для начала
- Starter ($7/месяц) - если нужна стабильность 24/7

### Шаг 3: Добавьте переменные окружения

В разделе **Environment Variables** добавьте:

| Key | Value |
|-----|-------|
| `NODE_ENV` | `production` |
| `PORT` | `10000` |
| `TELEGRAM_BOT_TOKEN` | `7248913086:AAHnu8f8bQfrOCWkwlkmnmjqs2JdTv2BHGU` |
| `TELEGRAM_CHAT_ID` | `-1003244054167` |
| `PUPPETEER_SKIP_CHROMIUM_DOWNLOAD` | `true` |

### Шаг 4: Деплой

1. Нажмите **"Create Web Service"**
2. Дождитесь окончания сборки (2-3 минуты)
3. Вы получите URL вида: `https://shanghai-schedule.onrender.com`

**Проверьте работу:**
```bash
curl https://shanghai-schedule.onrender.com/api/health
```

Должен вернуть:
```json
{
  "status": "ok",
  "time": "2025-11-06T...",
  "bot": true,
  "screenshots": "html2canvas"
}
```

---

## 🌐 Настройка домена на GoDaddy

### Шаг 1: Получите URL Render

После деплоя Render дает вам URL, например:
```
shanghai-schedule.onrender.com
```

### Шаг 2: Настройте DNS в GoDaddy

1. Зайдите в GoDaddy: https://dcc.godaddy.com
2. Найдите домен `escortwork.org`
3. Нажмите **"DNS"** или **"Manage DNS"**

### Шаг 3: Добавьте CNAME запись

Добавьте следующую запись:

| Type | Name | Value | TTL |
|------|------|-------|-----|
| CNAME | @ | shanghai-schedule.onrender.com | 600 |

**Важно:**
- Если есть старая A-запись для `@`, удалите её
- Если `@` не разрешен, используйте `www`

**Альтернативный вариант (если CNAME @ не работает):**

Используйте A-запись с IP Render:
1. Получите IP Render: `nslookup shanghai-schedule.onrender.com`
2. Добавьте A-запись:

| Type | Name | Value | TTL |
|------|------|-------|-----|
| A | @ | IP_ADDRESS | 600 |

### Шаг 4: Добавьте домен в Render

1. В Render Dashboard откройте ваш сервис
2. Перейдите в **Settings** → **Custom Domain**
3. Нажмите **"Add Custom Domain"**
4. Введите: `escortwork.org` (или `www.escortwork.org`)
5. Нажмите **"Save"**

### Шаг 5: Дождитесь распространения DNS

DNS обновление занимает от 15 минут до 48 часов (обычно 1-2 часа).

**Проверить статус:**
```bash
# Проверить DNS
nslookup escortwork.org

# Проверить домен
curl https://escortwork.org/api/health
```

### Шаг 6: Настройте SSL (HTTPS)

Render автоматически настроит бесплатный SSL сертификат от Let's Encrypt.

---

## 📦 Работа с GitHub

### Первоначальная настройка

Если вы еще не настроили Git:

```bash
git config --global user.name "Your Name"
git config --global user.email "your.email@example.com"
```

### Клонирование репозитория

```bash
cd ~/PycharmProjects
git clone https://github.com/Santazloy/she_v_1.0.git
cd she_v_1.0
```

### Проверка статуса

```bash
git status
git branch  # должен показать: * main
```

---

## 🔧 Обновление через PyCharm

### Метод 1: Через интерфейс PyCharm

1. **Открыть проект:**
   - File → Open → Выбрать папку `she_v_1.0`

2. **Внести изменения:**
   - Редактируйте файлы (index.html, server.js, и т.д.)
   - PyCharm подсветит измененные файлы синим

3. **Коммит изменений:**
   - VCS → Commit (или Ctrl+K / Cmd+K)
   - Выберите измененные файлы
   - Напишите сообщение коммита (например: "Добавлена новая таблица")
   - Нажмите **"Commit and Push"**

4. **Push на GitHub:**
   - Если не нажали "Commit and Push", используйте:
   - VCS → Git → Push (или Ctrl+Shift+K / Cmd+Shift+K)

5. **Автодеплой на Render:**
   - Render автоматически обнаружит изменения на GitHub
   - Начнется автоматическая пересборка (2-3 минуты)
   - Проверьте логи в Render Dashboard

### Метод 2: Через терминал PyCharm

1. **Открыть терминал в PyCharm:**
   - View → Tool Windows → Terminal (или Alt+F12)

2. **Добавить изменения:**
   ```bash
   git add .
   ```

3. **Создать коммит:**
   ```bash
   git commit -m "Описание изменений"
   ```

4. **Отправить на GitHub:**
   ```bash
   git push origin main
   ```

5. **Проверить деплой на Render:**
   ```bash
   # Подождать 2-3 минуты, затем проверить
   curl https://escortwork.org/api/health
   ```

### Типичные команды Git

```bash
# Проверить статус (какие файлы изменены)
git status

# Посмотреть последние коммиты
git log --oneline -5

# Посмотреть изменения в файле
git diff index.html

# Отменить изменения в файле (до коммита)
git checkout -- index.html

# Отменить последний коммит (но сохранить изменения)
git reset --soft HEAD~1

# Получить последние изменения с GitHub
git pull origin main

# Посмотреть ветки
git branch

# Создать новую ветку
git checkout -b feature-новая-функция

# Вернуться на main
git checkout main
```

---

## 🔍 Проверка работы

### 1. Локальная проверка

```bash
# Запустить сервер
npm start

# Открыть браузер
http://localhost:3000

# Проверить API
curl http://localhost:3000/api/health
```

### 2. Проверка на Render

```bash
# Health check
curl https://escortwork.org/api/health

# Проверить данные
curl https://escortwork.org/api/schedule
```

### 3. Проверка Telegram бота

В вашей Telegram группе напишите:
- `/start` - должен ответить приветствием
- `/all` - должен предложить использовать веб-интерфейс

### 4. Проверка скриншотов

1. Зайдите на https://escortwork.org
2. Войдите под любым пользователем
3. Нажмите кнопку 📸
4. Скриншот должен прийти в Telegram группу

---

## 🎮 Как использовать приложение

### Вход в систему

URL: https://escortwork.org

**Пользователи:**
- kris / 12344321
- misha / 12344321
- katya / 12344321
- wang / 12344321
- lion / 12344321

### Основные функции

**Кнопки внизу:**
- ➕ - Добавить таблицу (111, 222, 333, 555, 666, 888)
- 💾 - Сохранить данные
- 📋 - Показать журнал изменений
- 📸 - Отправить скриншот в Telegram

**Возможности:**
- Переключение между 3 днями (вкладки вверху)
- Добавление/удаление таблиц
- Ввод произвольного текста в ячейки
- Ввод адресов на китайском
- Автообновление данных каждые 2 секунды
- Журнал всех изменений

### Специальные символы

Вы можете вводить любой текст, включая:
- ✓ - выполнено
- ❌ - отменено
- ⚽ - футбол
- 🕐 - время
- И любые другие эмодзи

---

## 📱 Мобильная версия

Приложение полностью адаптировано для мобильных устройств:
- Touch-friendly интерфейс
- Оптимизированный размер шрифтов
- Удобный ввод данных
- Работает на iOS и Android

---

## ⏰ Автоматическое архивирование

**Каждый день в 4:00 по времени Шанхая:**
1. Удаляется расписание сегодняшнего дня
2. Таблицы сдвигаются (завтра → сегодня, послезавтра → завтра)
3. В Telegram приходит уведомление об архивировании

---

## 🐛 Решение проблем

### Проблема 1: Сайт не открывается

**Решение:**
```bash
# Проверьте статус Render
curl https://shanghai-schedule.onrender.com/api/health

# Проверьте DNS
nslookup escortwork.org

# Если DNS не работает, подождите 1-2 часа
```

### Проблема 2: Скриншоты не отправляются

**Причины:**
- Telegram bot токен неверный
- Chat ID неверный
- Проблемы с сетью

**Решение:**
- Проверьте переменные окружения в Render
- Проверьте логи в Render Dashboard

### Проблема 3: Данные не сохраняются

**Решение:**
- Проверьте, что файлы создаются: логи должны показать "Directories initialized"
- Render Free Tier может сбрасывать диск после перезапуска (это нормально)

### Проблема 4: Git push не работает

**Решение:**
```bash
# Проверьте аутентификацию
git config --list | grep user

# Если нужно, настройте снова
git config --global user.name "Your Name"
git config --global user.email "email@example.com"

# Попробуйте снова
git push origin main
```

---

## 📊 Мониторинг

### Логи Render

1. Зайдите в Render Dashboard
2. Выберите ваш сервис
3. Перейдите в **Logs**

**Что искать:**
- ✅ "Server running on port 10000"
- ✅ "Telegram bot started successfully"
- ✅ "Screenshot sent to Telegram"
- ⚠️ Любые ERROR сообщения

### Проверка статуса

```bash
# Health check
curl https://escortwork.org/api/health

# Должен вернуть
{
  "status": "ok",
  "time": "2025-11-06T...",
  "bot": true,
  "screenshots": "html2canvas"
}
```

---

## 🎯 Быстрый чеклист

- [ ] Клонирован репозиторий из GitHub
- [ ] Установлены зависимости (`npm install`)
- [ ] Создан `.env` файл с токенами
- [ ] Локально работает (`npm start`)
- [ ] Создан Web Service на Render
- [ ] Добавлены переменные окружения в Render
- [ ] Render успешно задеплоен
- [ ] Настроены DNS записи в GoDaddy (CNAME)
- [ ] Добавлен custom domain в Render
- [ ] Домен работает (https://escortwork.org)
- [ ] Telegram бот отвечает на команды
- [ ] Скриншоты отправляются в Telegram
- [ ] PyCharm подключен к репозиторию

---

## 🚀 Готово!

Ваше приложение полностью настроено и работает!

**URLs:**
- 🌐 Веб-интерфейс: https://escortwork.org
- 📦 Render Dashboard: https://dashboard.render.com
- 📁 GitHub: https://github.com/Santazloy/she_v_1.0
- 🤖 Telegram бот: уже настроен в группе

**При возникновении проблем:**
- Проверьте логи в Render Dashboard
- Проверьте переменные окружения
- Проверьте статус DNS на GoDaddy

**Для обновления:**
1. Внесите изменения в PyCharm
2. Commit + Push
3. Render автоматически обновит за 2-3 минуты